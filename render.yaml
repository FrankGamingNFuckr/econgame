services:
  - type: web
    name: econgame
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app_auth:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
